function join(arr, str) {
  let res = "";
  for (let i = 0; i < arr.length - 1; i++) {
    res += arr[i];
    res += str;
  }
  if (arr.length > 0) {
    res += arr[arr.length - 1];
  }

  console.log(res);
}
join([1, 2, 3, 4, 5], "=>");
